# Python pipeline

Run [`single_run.ipynb`](single_run.ipynb) from this folder. Full overview: [`../README.md`](../README.md).

| Path | Contents |
|------|----------|
| `src/` | Pipeline modules (steps 1–6) |
| `input_files/` | KB331 GDS + layermap (not in git if large) |
| `draft_output/` | Generated GDS / `.lyp` |
| `tests/` | KB331 regression tests — `python -m unittest discover -s tests` |
